const users = {
  name : "Ankit",
  email : "ankit.pndy2002@gmail.com",
  isActive: true
};

function createUser ({name : string, isActive: boolean}) {};

type user = {
  name : string,
  email : string,
  isActive: string
};

function newUser(){user : users}{}
